<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Login - FluxTV</title>
  <link rel="stylesheet" href="/public/styles.css">
</head>
<body class="min-h-screen bg-fluxtv-bg text-gray-100 font-body antialiased flex items-center justify-center px-4 py-12">
  <div class="w-full max-w-md bg-black/60 border border-white/10 rounded-3xl p-8 shadow-[0_0_45px_rgba(10,0,20,0.85)] space-y-6">
    <div class="flex items-center gap-3">
      <img src="/public/img/logo.png" alt="FluxTV" class="h-12 w-auto">
      <div>
        <p class="text-xs uppercase tracking-[0.5em] text-purple-200">FluxTV</p>
        <h1 class="text-2xl font-semibold text-white">Welcome Back</h1>
      </div>
    </div>

    <% if (error) { %>
      <div class="bg-gradient-to-r from-rose-500/90 to-orange-500/80 text-white text-sm p-3 rounded-2xl border border-white/10 shadow-lg shadow-rose-900/30">
        <%= error %>
      </div>
    <% } %>

    <form method="post" action="/login" class="space-y-4">
      <div>
        <label class="block text-sm font-medium mb-1 text-gray-200">Username</label>
        <input type="text" name="username" required
          class="w-full px-4 py-2.5 bg-black/70 border border-white/10 rounded-xl focus:ring-2 focus:ring-purple-500 focus:outline-none text-sm text-gray-100"
          placeholder="admin">
      </div>

      <div>
        <label class="block text-sm font-medium mb-1 text-gray-200">Password</label>
        <input type="password" name="password" required
          class="w-full px-4 py-2.5 bg-black/70 border border-white/10 rounded-xl focus:ring-2 focus:ring-purple-500 focus:outline-none text-sm text-gray-100"
          placeholder="••••••••">
      </div>

      <button type="submit"
        class="w-full inline-flex items-center justify-center px-4 py-2.5 rounded-xl bg-gradient-to-r from-purple-500 to-fuchsia-500 font-semibold shadow-neon hover:brightness-110 transition">
        Login
      </button>
    </form>

  </div>
</body>
</html>
