<!DOCTYPE html>
<html class="h-full bg-gray-900 text-gray-100">
<head>
  <meta charset="utf-8" />
  <title>First-Time Setup</title>
  <link rel="stylesheet" href="/public/styles.css">
</head>

<body class="h-full flex items-center justify-center px-4">

  <div class="w-full max-w-sm bg-gray-800 shadow-xl rounded-lg p-8 border border-gray-700">
    <h1 class="text-2xl font-semibold text-center mb-6">Create Admin Account</h1>

    <% if (error) { %>
      <div class="bg-red-700 text-white text-sm p-3 rounded mb-4 border border-red-900">
        <%= error %>
      </div>
    <% } %>

    <form method="POST" action="/setup-user/create" class="space-y-4">
      <div>
        <label class="block text-sm font-medium mb-1">Username</label>
        <input type="text" name="username" required
          class="w-full px-3 py-2 bg-gray-900 border border-gray-700 rounded focus:ring-2 focus:ring-pink-500 focus:outline-none" />
      </div>

      <div>
        <label class="block text-sm font-medium mb-1">Password</label>
        <input type="password" name="password" required
          class="w-full px-3 py-2 bg-gray-900 border border-gray-700 rounded focus:ring-2 focus:ring-pink-500 focus:outline-none" />
      </div>

      <button type="submit"
        class="w-full py-2 bg-pink-600 hover:bg-pink-700 rounded-md text-white font-semibold transition">
        Create Account
      </button>
    </form>
  </div>

</body>
</html>